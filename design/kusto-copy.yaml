source:
    clusterQueryUri:  https://inttestsyenycav4i2vma.eastus.kusto.windows.net
    concurrentQueryCount:  1    #   Number of queries / commands sent in parallel to a given cluster
    concurrentExportCommandCount:  2    #   Number of export commands sent in parallel to a given cluster
    includeDatabases:   #   Optional ; by default every dbs are included
    - db1
    - db2
    excludeDatabases:   #   Optional ; by default no db is excluded ; can't be used 
    - db3
    databaseOverrides:
    -   name:  github
        destinationName:  githubCopy
        includeTables:
        - table1
        - table2
        excludeTables:
        - table3
        #   + everything under "databaseDefault" can override at the db level
destinations:
    -   clusterQueryUri:  https://inttestsyenycav4i2vma.eastus.kusto.windows.net
        clusterIngestionUri:  https://ingest-inttestsyenycav4i2vma.eastus.kusto.windows.net
        concurrentQueryCount:  2    #   Number of queries / commands sent in parallel to a given cluster
        isEnabled:  true #   Indicates if data will be sent to the destination
databaseDefault:
    isEnabled:  true #   Indicates if data will be exported from the source
    maxRowsPerTablePerIteration:  10000000
    copyUntil:  now #   Options are 'now' and 'forever'
    backfillHorizon:  null  #   Horizon back in time we replicate history (to be put in a 'ago', e.g.  3d) ; null means infinite
    rpo:  60  #   In minutes

# CLI parameters:

#   Storage Account
#   Storage Account Access Keys
#   Tenant ID
#   App ID
#   Secret